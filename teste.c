#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct stack{
    char wagons[26];


} stack;

void push(struct stack*s ){

}

void pop(struct stack*s){

}

int main (){

    int total=0;

    while(scanf("%d", total)>=1){
        char input[total];
        char output[total];

        scanf("%[^\n]", input);
        scanf("%[^\n]", output);



    }
    return 0;
}
